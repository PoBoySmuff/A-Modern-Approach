/*Outputs several Int and Float variables without initialization. Expect different results on each run*/
#include <stdio.h>

int main(void)
{
	int a, b, c;
	float g,h,i;
	
	printf("Int values: %d, %d, %d\n", a, b, c);
	printf("Float values: %f, %f, %f\n", g, h, i);
	
	return 0;
}
